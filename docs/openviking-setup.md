# OpenViking Setup Notes

This file records known working OpenViking setup commands.

## Expected Local Service

The extension expects a local service URL:

```text
http://127.0.0.1:1933
```

## To Verify

Contributors should verify:

- OS and version
- Python version
- OpenViking package version
- install command
- server startup command
- health endpoint
- memory search endpoint
- session append endpoint
- session commit endpoint

## Template

```text
OS:
Python:
OpenViking version:
Install command:
Start command:
Health endpoint:
Search endpoint:
Append endpoint:
Commit endpoint:
Notes:
```

